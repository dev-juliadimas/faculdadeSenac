## 3. 🔄 `lacosRepeticao/README.md`

Este `README` é o mais importante em termos de documentar o controle de fluxo, pois lida com `for`, `while`, e `do-while`.

```markdown
# 🔄 Laços de Repetição e Controle de Fluxo (18 Projetos)

Esta pasta é dedicada ao **controle de fluxo e repetição**. Os exercícios exploram a aplicação dos laços **`for`**, **`while`** e **`do-while`** para diversas finalidades, desde contagens simples até acumulação de valores e validação de dados.

## 🎯 Conceitos Chave

* **`for` (Limites e Passos):** Contagens com passos personalizados (`i += 2`, `i -= 5`), cálculo de fatorial e identificação de múltiplos.
* **`while` (Condição Aberta):** Acúmulo condicional (parar com número negativo), validação de senha e iteração em intervalos.
* **`do-while` (Execução Mínima):** Menus interativos, validação de dados (repetir até ser válido) e jogos de adivinhação.

## 📁 Exemplos de Projetos

| Arquivo | Tipo de Laço | Lógica Implementada |
| :--- | :--- | :--- |
| `numValido.java` | **`do-while`** | Repete até o usuário inserir um número entre 1 e 100. |
| `pedir10somar.java` | **`for`** | Pede exatamente 10 números e calcula a soma total. |
| `numPositSomaNegPara.java` | **`while`** | Soma números positivos e para ao receber um número negativo. |
| `sequenciaFibonacci.java` | **`for`** | Gera os 10 primeiros números da série Fibonacci. |
| `tabuada.java` | **`do-while`** e **`for`** | Menu de tabuada interativo que repete até o usuário sair. |
| **... (Adicionar o restante dos 13 arquivos)** | | |

## 🚀 Como Executar

Para executar qualquer projeto, navegue até esta pasta no terminal e siga os passos (substitua pelo nome do seu arquivo):

```bash
cd lacosRepeticao/
javac NomeDoArquivo.java
java NomeDoArquivo
````

````

---

